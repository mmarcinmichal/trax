schedule:
  multifactor:
    _target_: "trax.learning.supervised.lr_schedules.multifactor"
    constant: 0.001
    factors: "constant * linear_warmup * cosine_decay"
    warmup_steps: 5000
    steps_per_cycle: 100000
