train:
  eval_frequency: 20
  eval_steps: 10
  inputs: "@data.make_inputs"
  loss_fn: "@bert.BERTPretrainingLoss()"
  metrics:
    loss: "@bert.BERTPretrainingLoss()"
  model: "@trax.models.BERT"
  optimizer: "@trax.optimizers.Adam"
  steps: 200000
