defaults:
  - base
  - _self_

data:
  sequence_copy_inputs:
    _target_: "trax.data.sequence_copy_inputs"
    _partial_: true
    vocab_size: 32
    batch_size: 512
    train_length: 16
    eval_min_length: 2
    eval_max_length: 16
    reverse: false
