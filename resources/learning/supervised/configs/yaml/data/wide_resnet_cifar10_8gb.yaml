data:
  batcher:
    _target_: "trax.data.batcher"
    data_streams: "${data.data_streams}"
    batch_size_per_device: 256
    bucket_length: 32
    buckets: null
    eval_batch_size: 512
    variable_shapes: false
  data_streams:
    _target_: "trax.data.data_streams"
    _partial_: true
    data_dir: null
    dataset_name: "cifar10"
    preprocess_fn: "${data.cifar10_augmentation_preprocess}"
  cifar10_augmentation_preprocess:
    _target_: "trax.data.cifar10_augmentation_preprocess"
    _partial_: true
