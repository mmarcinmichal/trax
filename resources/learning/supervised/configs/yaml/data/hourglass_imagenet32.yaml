data:
  batcher:
    _target_: "trax.data.batcher"
    data_streams: "${data.data_streams}"
    batch_size_per_device: 1
    eval_batch_size: 8
    max_eval_length: 3072
    variable_shapes: false
  data_streams:
    _target_: "trax.data.data_streams"
    _partial_: true
    data_dir: null
    dataset_name: "downsampled_imagenet/32x32"
    input_name: "image"
    target_name: "image"
    bare_preprocess_fn: "${data.downsampled_imagenet_flatten_bare_preprocess}"
  downsampled_imagenet_flatten_bare_preprocess:
    _target_: "trax.data.downsampled_imagenet_flatten_bare_preprocess"
    _partial_: true
