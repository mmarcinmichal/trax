defaults:
  - /data/base
  - _self_

data:
  make_streams:
    _target_: "trax.data.make_streams"
    _partial_: true
    train_stream:
      - "${data.train.TFDS}"
      - "${data.BairRobotPushingPreprocess}"
      - "${data.DictToTupleTargetsMask}"
      - "${data.ShuffleSmall}"
      - "${data.train.BucketByLength}"
      - "${data.AddLossWeights}"
    eval_stream:
      - "${data.eval.TFDS}"
      - "${data.BairRobotPushingPreprocess}"
      - "${data.DictToTupleTargetsMask}"
      - "${data.ShuffleSmall}"
      - "${data.eval.BucketByLength}"
      - "${data.AddLossWeights}"
  train:
    TFDS:
      _target_: "trax.data.TFDS"
      dataset_name: "t2t_video_bair_robot_pushing"
      data_dir: null
      train: true
    BucketByLength:
      _target_: "trax.data.BucketByLength"
      boundaries:
        - 8
        - 16
        - 32
        - 64
        - 128
        - 256
        - 512
        - 1024
      batch_sizes:
        - 4
        - 2
        - 1
        - 1
        - 1
        - 1
        - 1
        - 1
      length_keys:
        - 0
        - 1
  eval:
    TFDS:
      _target_: "trax.data.TFDS"
      dataset_name: "t2t_video_bair_robot_pushing"
      data_dir: null
      train: false
    BucketByLength:
      _target_: "trax.data.BucketByLength"
      boundaries:
        - 8
        - 16
        - 32
        - 64
        - 128
        - 256
        - 512
        - 1024
      batch_sizes:
        - 32
        - 16
        - 8
        - 4
        - 2
        - 1
        - 1
        - 1
      length_keys:
        - 0
        - 1
  AddLossWeights:
    _target_: "trax.data.add_loss_weights"
    _partial_: true
  DictToTupleTargetsMask:
    _target_: "trax.data.DictToTuple"
    keys:
      - "targets"
      - "targets"
      - "mask"
  ShuffleSmall:
    _target_: "trax.data.Shuffle"
    queue_size: 8
  BairRobotPushingPreprocess:
    _target_: "trax.data.BairRobotPushingPreprocess"
