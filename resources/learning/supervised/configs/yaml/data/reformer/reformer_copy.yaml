defaults:
  - /data/base
  - _self_

data:
  sequence_copy_inputs:
    _target_: "trax.data.sequence_copy_inputs"
    _partial_: true
    vocab_size: 13
    batch_size: 128
    train_length: 64
    eval_min_length: 4
    eval_max_length: 64
    reverse: false
    pad_to_multiple: 32
